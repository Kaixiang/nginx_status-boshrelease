---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - nginx_status
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: nginx-status
    version: latest
